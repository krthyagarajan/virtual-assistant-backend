spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://localhost:3306/virtual-assistant-backend
    username: root
    password: root
  jpa:
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect